/*
This is the recursive fibonacci algorithm.
It's much slower than the iterative fibonacci,
but I thought it would be good as an illustration.

*/

int fibonacci(int n){
    if( n == 0 ){
        return 0;
    }
    else{
        if (n == 1){
            return 1;
        }
    }

    return (fibonacci(n - 1) + fibonacci(n - 2));
}

void main(void){
    int x;
    x = input();
    output(fibonacci(x));
}